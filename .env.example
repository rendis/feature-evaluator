# =============================================================================
# Feature Evaluator — Configuration
# Copy this file to .env and adjust values for your environment.
# Single source of truth for both backend (server/) and frontend (console/).
# =============================================================================

# --- Server ---
SERVER_PORT=8080                              # HTTP port
SERVER_SHUTDOWN_TIMEOUT=10s                   # Graceful shutdown timeout

# --- PostgreSQL ---
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres?sslmode=disable
POSTGRES_MAX_CONNS=20
POSTGRES_MIN_CONNS=2
POSTGRES_MAX_CONN_LIFETIME=30m
POSTGRES_MAX_CONN_IDLE_TIME=5m
POSTGRES_HEALTHCHECK_PERIOD=1m
POSTGRES_CONNECT_TIMEOUT=10s

# --- Redis ---
REDIS_URI=localhost:6379                      # Redis address (host:port)

# --- Auth ---
AUTH_DISABLED=false                           # true = skip JWT validation, use dev user below
DEV_USER_EMAIL=dev@local.dev                  # Dev user email (only when AUTH_DISABLED=true)
DEV_USER_ROLE=owner                           # Dev user role: owner|admin|editor|viewer
AUTH_SECRETS_MASTER_KEY=                      # Base64-encoded 32-byte AES key for API-key encryption

# --- OIDC (required when AUTH_DISABLED=false) ---
OIDC_ISSUER=                                  # OIDC provider issuer URL (e.g. https://auth.example.com)
OIDC_AUDIENCE=                                # Expected JWT audience

# --- CORS ---
CORS_ALLOW_ORIGINS=http://localhost:5173      # Bootstrap/inherited origins (UI can add app-managed origins)

# --- Rate Limiting (GCRA via Redis, fail-open) ---
RATE_LIMIT_EVAL=500                           # Max eval requests per second
RATE_LIMIT_ADMIN=60                           # Max admin requests per second

# --- Logging ---
LOG_LEVEL=info                                # debug|info|warn|error
LOG_FORMAT=text                               # text (dev) | json (prod)

# --- Console / Vite (only VITE_* vars are exposed to the browser) ---
VITE_API_URL=http://localhost:8080/features   # Backend base URL
VITE_AUTH_DISABLED=true                       # true = bypass OIDC, use backend dev user

# --- OIDC for Console (required when VITE_AUTH_DISABLED is not "true") ---
VITE_OIDC_AUTHORITY=                          # OIDC provider URL
VITE_OIDC_CLIENT_ID=                          # OIDC client ID
VITE_OIDC_REDIRECT_URI=                       # Callback URL (default: {origin}/auth/callback)
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=           # Post-logout URL (default: {origin})
